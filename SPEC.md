# SMOS 기획안

Sub-Merchant Onboarding Status — 서브 머천트 온보딩 현황 보드

---

## 1. 왜 만들었나 (Problem)

SentBe의 Sales 팀은 서브 머천트 온보딩 상태를 확인하려면 운영팀에 직접 물어보거나 구글 시트를 뒤져야 했다. 시트는 컬럼이 많고 모바일에서 보기 어렵고, 어떤 머천트가 어느 단계에서 막혀있는지 한눈에 파악할 수 없었다.

## 2. 누구를 위한 앱인가 (Users)

| 사용자 | 핵심 니즈 | 사용 빈도 |
|--------|----------|----------|
| **Sales 팀** (주 사용자) | 머천트 이름/MID 검색 → 현재 단계·실패사유·VA 발급 즉시 확인 | 수시 (하루 여러 번, 폰으로) |
| **Sales 리드** | 전체 온보딩 현황 요약, 주간 보고, 클라이언트별 승인율 | 주 1~2회 |
| **운영팀** (간접) | Slack 알림으로 상태 변경 인지, 시트에서 직접 작업 | 실시간 알림 수신 |

## 3. 핵심 가치 (Core Value)

> **"머천트 이름 치면 3초 안에 온보딩 어디까지 왔는지 안다"**

이것이 SMOS의 존재 이유. 머천트 조회 기능은 모두 이 문장에 부합하는지로 판단한다.

### 두 번째 일: 분석 (v7.1~)
입금·집중도·추이·SLA 뷰는 위 문장에 기여하지 않는다. **대상이 다르다** —
세일즈 담당자의 조회가 아니라 **리드·보고용 집계**다. v7.1에서 `Concentration`·`Trend`를
넣으면서 이 선을 넘었으므로, 앞으로 기능을 판단할 기준을 둘로 나눠 적는다.

| | 조회 | 분석 |
|---|---|---|
| 대상 | 세일즈 담당자 | 리드, 보고 |
| 판단 기준 | 3초 안에 답이 나오나 | 결정을 바꿀 숫자인가 |
| 위치 | `Merchants` 목록 | `Concentration`·`Trend`·`SLA` 탭, Slack |

분석 기능은 **조회를 느리게 하거나 복잡하게 만들면 안 된다.** 별도 탭에 두는 이유가 이것이다.

## 4. 아키텍처

```
Google Sheets (원천 데이터)
    ↓ IMPORTRANGE
운영 시트 (KRW·VND 탭)
    ↓ doGet API
Apps Script (Code.gs)  ←→  Slack (알림)
    ↓ JSON
index.html (GitHub Pages)
    ↑ 30초 폴링
Sales 폰/PC
```

- **단일 파일**: `index.html` 하나. CSS/JS 인라인. 빌드 없음.
- **데이터 방향**: 시트 → 보드 (읽기 전용). 보드 → 시트 쓰기는 없음.
- **인증**: Google Sign-In (sentbe.com 도메인 제한)
- **호스팅**: GitHub Pages (`smos-deploy` 레포)

## 5. 코리도 (Corridor)

### KRW
- **상태 흐름**: KYC → Ops confirming → Succeeded / Failed
- **Steps**: Request → KYB → Review → VA 발급
- **주요 필드**: Client, 상점명, MID, 요청일, 티켓, 상태, 실패사유, VA, VA상태, 메모

### VND
- **상태 흐름**: In progress → Approved / Rejected·Offboarded
- **Steps**: Request → KYB → Review → VA 발급
- **주요 필드**: MID, 상점명, FI Merchant, FI Partner, 은행명, 요청일, 상태, VA, VA발급일, KYC일, Jira

## 6. 구현 완료 기능

### v1 — 기본 보드
- [x] KRW/VND 탭 전환
- [x] 검색 (상점명·고객명·MID)
- [x] 상태별 필터 칩
- [x] 클라이언트별 그룹 접기/펼치기
- [x] 머천트 상세 펼치기 (step tracker, 전체 필드)
- [x] VA 번호 복사
- [x] 머천트 요약 복사 (공유용)

### v2 — 대시보드·다크모드
- [x] 대시보드 (주간 신규, 주간 승인율, 클라이언트별, 월간 추이, 승인율 추이)
- [x] 다크 테마 토글
- [x] 정렬 (그룹순/최근순)
- [x] CSV 내보내기
- [x] JSON 백업

### v3 — 인증·보안
- [x] Google Sign-In (sentbe.com 도메인 제한)
- [x] Users 탭 기반 role 관리 (admin/sales/ops)
- [x] 관리 모드 (admin만 편집)
- [x] JWT 검증
- [x] GitHub Actions 보안 스캔

### v4 — 이메일·Forecast
- [x] 주간 보고 이메일 (HTML 템플릿)
- [x] Ops/Sales 분리 이메일
- [x] Forecast 뷰 (KRW/VND 별도 탭)
- [x] 한국어/영어 전환

### v5 — Slack 연동
- [x] 상태 변경 Slack 알림 (1분 폴링 방식)
- [x] 5가지 알림: 신규, 실패, 철회, 승인, 진행상태 변경
- [x] 주간 보고 Slack 발송

## 7. 다음 개발 우선순위

P1·P2(Funnel Analytics)는 v6.0에서 완료. 아래는 **온보딩 효율** 로드맵으로,
시트에 있으면서 앱이 읽지 않는 데이터를 기준으로 정했다 (2026-09-11 조사).

### 근거: 시트에 있는데 앱이 안 읽는 컬럼

| 컬럼 | 채워짐 | 쓸 수 있는 곳 |
|---|---|---|
| KRW `SLA` (시트가 이미 계산) | 111 / 116 | ✅ v7.9 `Lead Time` — 백엔드 계산 불필요 |
| KRW `Compliance approval or failure.` | 108 | ✅ v7.9 판정일 (월별 축 + 활성화 기준일) |
| VND `Date of L1/L2/L3 Completion` | 42 / 32 / 71 | 단계별 체류일 — **근거 부족** |
| VND `Sign off` ×2 | Jack 56, Konstantin 48 | **비목표** (§8) |

KRW `SLA` 분포: 최소 1일 · 최대 191일 · 평균 15.4일 (111건, SENTBE TEST 제외).
클라이언트별 평균은 GEP 52.6일 vs Jivara 1.2일로 **40배** 차이.
평균이 아니라 **중위값·p90**으로 보여줘야 한다 — 191일 한 건이 평균을 혼자 끌어올린다.

### Priority 1: 장기 체류 알림 ✅ v7.4 완료
- 같은 상태로 14일 이상 머문 건을 Slack 알림. 주간 트리거(월 09:30 KST, v7.12에서 일일→주간)이고
  **새로 단계(14→28→56→112일)를 넘은 건만** 보낸다
- 기준 14일 선정 근거: 미완결 8건의 경과일이 108·23·23·15·2·1·1·1일 →
  7일 4건 / 14일 4건 / 21일 3건 / 30일 1건. SLA 대부분이 1~3일에 끝나므로 14일이면 방치 건만 걸러진다
- **알림 반복은 사람의 처리와 무관하게 시스템이 판단한다** — 상세는 v7.4 배포 기록 참고

### 알림 설계 원칙 (v7.4에서 확립)
경보를 추가할 때마다 이 세 줄을 먼저 확인한다.
1. **행동 가능** — 받는 사람이 할 일이 있을 때만 보낸다. "볼륨이 줄었다"는 행동이 아니다
2. **반복 억제는 시스템 몫** — 조건이 유지되는 동안 같은 내용을 다시 보내지 않는다.
   "사람이 처리하면 빠진다"에 기대면 안 된다. 단계 승격 + 스냅샷으로 판단한다
3. **침묵의 가치** — 보낼 게 없는 날은 보내지 않는다. 매일 "0건"이 오면 채널이 음소거된다

### Priority 2: SLA 뷰 ✅ v7.9 완료 (`Lead Time` 탭 1부)
### Priority 3: 활성화 리드타임 ✅ v7.9 완료 (`Lead Time` 탭 2부) · ✅ v7.13 주간 보고 `활성화` 섹션
둘을 별도 탭으로 나누지 않고 **한 탭에 앞뒤 구간으로 합쳐** 배포했다 — 상세는 v7.9 기록 참고.
`조용해짐` 칩도 같이 나갔다 (입금 중단 경보를 보드에서 보이게 한 것).

### Priority 4: 예측 대비 실제
**목표**: 우리 예측은 쓸 만한가
- **선행 작업이 앱 밖에 있다.** 현재 VND 예측 540 / 실제 83 (달성률 15.4%)으로
  월 60 균일 배분이 현실과 6.5배 차이다. 달성률을 키우면 매달 빨간 숫자만 남는다
- 예측을 실적 기반으로 다시 세우는 게 먼저고, 그건 시트 작업이다
- 앱에서는 달성률보다 **예측 오차의 추세**("예측이 맞아가고 있나")가 답할 가치가 있는 질문

### Priority 5: 단계별 체류일 — 데이터 대기
**목표**: Request → KYC → L1 → L2 → L3 → VA 중 어디서 막히나
- 지금 만들 기능이 아니라 **지금 정할 입력 규칙**이다. L2가 32/93이면
  "L2가 느린 건지, L2 날짜를 안 적은 건지" 구분이 안 된다
- 두 가지 길이 있고 아직 결정되지 않았다:
  1. **시트 입력 규칙** — 사람이 L1·L2·L3 날짜를 빠짐없이 적는다. 2~3개월 쌓인 뒤 가능
  2. **상태 전이 자동 기록** — `pollStatusChanges()`가 이미 1분마다 상태를 읽고 스냅샷과 비교한다.
     변화를 저장만 하면 오늘부터 이력이 쌓인다. 단 해상도가 `Onboarding Status` 수준이고,
     §8의 `활동 로그: 불필요`와 검토가 필요하다 (머천트 상태 이력이지 사람 활동 로그는 아니다)
- 현재 step tracker는 상태값에서 단계를 **추정**하고 있다. 이 데이터가 쌓이면 실측으로 바꿀 수 있다

### 선행 과제 (앱 밖)
- VND 온보딩 탭 빈 프리필 **50,405행** — 매 요청마다 읽는다. 응답 40초 중 상당 부분
- KRW `MCID000470` 중복 2행 — 점유율 중복은 코드로 막았지만 Slack 알림 유실은 못 막는다
- VND 거래 13건 $150,356 수취인명 공란 — 머천트 귀속 불가. `Trend`에 주황으로 분리 표시 중

### Backlog
- 코리도 추가 (JPY 등) — 필요 시
- 머천트 셀프서비스 포털 — 회사 내부 개발 중이므로 보류
- 자동화 (온보딩 프로세스) — SMOS 역할 범위 재검토 필요
- 팀 담당자별 뷰 — 추후

## 8. 하지 않는 것 (Non-Goals)

- **양방향 데이터 쓰기**: 시트가 원천. SMOS에서 시트로 쓰기는 하지 않음. 운영팀도 Sales도 SMOS에 직접 입력할 일 없음 (시트에서 작업).
- **머천트별 실시간 SLA 타이머**: 운영팀 내부 관리 영역. 건별 카운트다운은 SMOS에 노출하지 않는다.
  단 **집계 SLA 리포트**(중위값·p90·클라이언트별·추세)는 리드 보고용으로 만든다 — v7.4에서 범위를 좁혔다.
  손으로 만들던 보고서를 자동화하는 것이고, 운영팀 업무 도구와는 다른 물건이다.
- **심사자별 처리량·처리 속도**: 만들지 않는다. `Sign off` 컬럼에 데이터가 있고 기술적으로 가능하지만,
  심사자가 소수인 조직에서 사람별 지표를 상시 노출하면 측정되는 순간 행동이 바뀐다
  (어려운 건을 미루거나 서인을 늦게 적는 방향). 병목은 사람이 아니라 **단계**를 재서 찾는다.
- **활동 로그**: 불필요.
- **빌드 시스템 도입**: 단일 파일 유지. webpack, vite 등 도입 금지.
- **localStorage/sessionStorage**: 아티팩트 환경에서 깨짐. 상태는 메모리 + 백엔드만.

## 9. 기술 제약

- 단일 HTML 파일 (CSS/JS 인라인)
- 외부 의존성: Pretendard CDN, Google Sign-In SDK만 허용
- 모바일 우선 (입력 폰트 16px 이상)
- 색상은 CSS 변수 (`--s-*`)
- UI 문구 한국어 / 데이터 값은 시트 원본 그대로
- 코리도 차이는 `CORRIDORS` config에서만 처리 (하드코딩 분기 금지)

## 10. 핵심 URL

| 항목 | URL |
|------|-----|
| GitHub Pages | https://sentbejack.github.io/smos/ |
| Apps Script | `AKfycbwLnplb...AApTV-yQ` (현재 배포, v7.10) — 정본은 `index.html`의 `BACKEND_URL` |
| Google Sheet | `18duKGwDXB0V...7U2hE` |
| Slack Channel | #smos-alerts |

---

## 배포 기록 (Changelog)

### v7.14 — 2026-09-12
**디자인 정비.** 정보 구조와 기능은 그대로 두고 표현만 바꿨다 — 매일 쓰는 도구라
익숙한 것의 위치가 바뀌면 그 자체가 손해다. 측정 결과: **한 화면에 보이는 머천트 6곳 → 9곳(+50%)**,
행 높이 143px → 100px, 헤더 316px 유지.

- **지면에서 파랑을 뺐다.** 바탕 `#F2F6FF` · 테두리 `#D6E2F8`가 모두 강조색과 같은 계열이라
  정작 강조가 배경에 묻혀 있었다. 중립 회색(`#F6F7F9`/`#E6E8EC`)으로 바꾸고 파랑은 강조에만 쓴다
- **목록의 상태 배지를 채우지 않는다.** 127행 중 80행이 `Succeeded`인데 칠해진 초록 알약이
  80번 반복되면 그건 정보가 아니라 배경이다. 점 + 글자로 바꿨다.
  상세에서는 하나뿐이라 칠한 알약이 제 역할을 하므로 그대로 둔다(`statusBadge(v, quiet)`)
- **접힌 행에서 트래커 라벨을 숨긴다.** `요청·KYB·심사·VA 발급`은 모든 행이 똑같다 —
  행마다 반복하면 범례를 127번 인쇄하는 것이다. 점은 행마다 다르므로 남기고,
  폭을 108px로 좁혀 왼쪽에 붙였다(전폭 레일은 행 사이 구분선으로 읽혀 목록이 토막 나 보였다).
  펼치면 라벨이 그대로 나온다
- **점유율·건수·최근일을 한 줄로 합쳤다.** 오른쪽이 3줄, 왼쪽(이름·MID)이 2줄이라
  그 틈이 행 한가운데 빈 공간으로 남아 있었다
- **선택 표현에 위계를 줬다**: 코리도(채움) > 뷰(분절) > 필터 칩(연한 강조).
  전부 검정 알약이면 무엇이 더 중요한 선택인지가 화면에서 사라진다
- 머천트 이름 15px/650 — 이 화면의 주인공이다("이름 치면 3초 안에"). 점유율은 강조색에서 muted로.
  강조색은 누를 수 있는 것과 지금 선택된 것에만 쓴다
- 숫자는 전부 등폭(`tabular-nums`)으로 자리를 맞춘다. 금액이 세로로 비교되는 앱이다
- 카드 그림자를 거의 없앴다. 테두리 + 그림자 + 색조가 겹쳐 목록이 층층이 떠 보였다

#### 접근성 — 이번에 드러난 기존 문제
대비 검사(2테마 × 2코리도 × 6뷰 = **24조합**)를 돌려 WCAG AA 미달을 전수 조사했다.
**114곳이 미달이었고 대부분 이전부터 있던 문제였다.** 전부 고쳐 24조합 0건.
- `--muted`가 흰 바탕에서 **3.0:1** (기준 4.5:1) — 보조 텍스트 전부가 여기 걸렸다. `#676D7A`로
- 뷰 전환의 비활성 항목이 3.0:1 — 주 내비게이션이 겨우 읽히면 안 된다. `--ink-2`로
- `입금 전환 58%`의 초록이 **2.0:1** — 밝은 초록(`-d`)은 점·막대용이다. 글자엔 `-t`를 쓴다
- 다크에서 선택된 코리도 탭이 **2.7:1** — 다크 강조색이 밝은 파랑이라 흰 글자를 얹으면 안 된다.
  진한 파랑(`#2B5CF6`)으로 칠한다
- 검증: 24조합 렌더 에러 0 · 가로 오버플로 0 · 검색·필터·펼침·표 클릭 이동·CSV 모두 정상

### v7.13 — 2026-09-12
로드맵 4번 완료. **주간 보고에 `활성화` 섹션 추가** — 온보딩은 끝났는데 입금이 없는 곳.

- 입금 중단 경보가 "오다가 멈춘 곳"을 맡고 이쪽은 **"한 번도 오지 않은 곳"**을 맡는다.
  둘은 세일즈가 할 말이 다르다 — 전자는 이탈 방어, 후자는 시작이 막힌 이유 찾기다
- **개별 명단보다 클라이언트별 비율을 먼저 싣는다.** 27곳에 27통 전화하는 일은 일어나지 않지만
  "이 FI의 42곳 중 28곳이 VA만 받고 안 쓴다"는 그 FI와의 대화 한 번이고 그건 실제로 일어난다.
  대기 많은 순 상위 4개 클라이언트 + 가장 오래된 머천트 3곳 + "외 N곳"
- 기준일은 `MAP[key].actAnchor` (KRW 심사 판정일 / VND VA 발급일).
  **앱의 `CORRIDORS.actAnchor`와 같은 값**이어야 화면과 보고가 어긋나지 않는다.
  `ACTIVATION_DAYS = 30`도 앱 `Lead Time` 탭의 30일과 짝이다
- **분모는 "기준일이 있는 건"으로 통일.** 클라이언트별만 전체 승인으로 세면 바로 윗줄의
  코리도 비율과 분모가 달라 숫자가 안 맞아 보인다 (하니스에 불변식으로 고정: 클라이언트 분모 합 = 코리도 분모)
- 기준일 공란·미래 날짜는 판정하지 않는다. 추정하지 않는다
- **입금 중단은 한 줄 요약만**(곳수 · 금액 · 비중). 명단은 같은 날 09:40 경보가 담당하고
  그쪽은 새로 악화된 것만 보낸다 — 재고(stock)와 증분(delta)을 나눠 중복을 피한다
- **`previewWeeklyReport()`** 추가 — 발송 없이 로그로만 본다.
  `dryRun === true`로 엄격 비교한다: 시간 기반 트리거는 첫 인자로 이벤트 객체를 넘기는데
  그게 truthy라 느슨하게 비교하면 매주 조용히 발송을 건너뛴다 (하니스가 이 경로를 검증한다)
- 주간 보고가 이제 입금 원천을 읽는다(`attachDeposits_`). 캐시(300s)를 타므로 추가 부하는 없다
- 검증: 하니스 23건 통과. 렌더된 메시지 본문까지 확인했고, 기준일 공란·30일 미만 신규 승인·
  철회/심사중 제외·트리거 이벤트 인자 경로가 모두 포함된다

#### 이름 조인의 위험 (확인함, 현재 무해)
VND는 머천트명으로 조인하므로 `normName_`(대문자·영숫자만)에서 서로 다른 머천트가
같은 키로 접힐 수 있다. 실데이터 점검 결과:
- 온보딩 탭 충돌 **1건** — `SENDIT TECHNOLOGY LIMITED`(VN0099·VN0134)이고 **같은 이름의 중복 행**이다.
  둘 다 `Rejected / Offboarded`라 승인 집합에 없어 활성화율에 영향이 없다
- RAW 표기 변형 11건(`CLOUD WAVE HONG KONG LIMITED` / `SB CLOUD WAVE ...` / `CLOUD WAVE (HONG KONG) LIMITED`)은
  **정규화가 의도대로 접고 있는 것**이다. 버그가 아니라 기능
- 서로 다른 머천트가 잘못 합쳐진 사례는 없다. 이름이 비슷한 신규 머천트가 들어오면 재점검할 것

### v7.12 — 2026-09-12
- **장기 체류·입금 중단 경보를 일일 → 주 1회(월 09:30 / 09:40 KST)로 변경**
  - 임계가 14·30·60일짜리 신호라 하루 단위 배달이 필요 없다
  - 단계 승격 억제 덕에 일일이어도 조용한 날이 대부분이었다(현재 데이터로 30일 중 약 8일).
    문제는 소음이 아니라 **그 8일이 1~2건짜리로 흩어진다는 것** — 그런 알림은 읽히지 않는다.
    한 주치를 모으면 같은 내용이 읽히는 분량으로 온다
  - 주간 보고(월 09:10) 직후에 놓아 월요일 아침 한 묶음으로 읽히게 한다
  - **잃는 것**: 화요일에 단계를 넘은 건은 다음 월요일에 알려진다(최대 6일 지연).
    두 경보 모두 이미 2주 이상 지난 사건이라 6일이 판단을 바꾸지 않는다
  - 억제 로직은 그대로다. 한 주 사이에 14일과 28일을 모두 넘은 건은 28일 단계로 **한 번만**
    알린다 — `escalationStep_`이 발송 시점의 경과일로 단계를 정하므로 밀린 알림이 쌓이지 않는다
- 되돌리려면 `createSlackTriggers()`의 `.onWeekDay(...MONDAY)`를 `.everyDays(1)`로 바꾸면 된다

### v7.11 — 2026-09-11
**정합성 검증**(시트 원천 → 백엔드 페이로드 → 화면)에서 드러난 결함 수정.
검증 방법: 실계정으로 SMOS를 띄워 `Store.data`(백엔드 실제 응답)를 읽고,
시트를 따로 읽어 백엔드 규칙을 재현한 값과 대조.

- **VND 상태값 19행이 프론트 설정에 없었다** — `CORRIDORS.VND.statuses`에
  `In progress`(공백)만 있고 시트 실제 표기는 `In-progress`(하이픈)이며 `Withdrawn`은 아예 없었다.
  `Code.gs`의 `CORRIDOR_STATUSES`는 둘 다 알고 있어 **백엔드는 정상**이었고 프론트만 어긋났다
  - 증상: **상태 필터 칩 합계 74 ≠ 행수 93.** `Withdrawn` 15행 + `In-progress` 4행(20%)에
    필터로 도달할 수 없었다. 상태 배지는 설정 tone이 아닌 폴백 색으로 나왔다
  - `stepOf()`가 `"In progress"` 문자열 비교라 `In-progress` 4행의 step tracker가
    요청 단계만 완료로 표시했다 → 하이픈·공백을 같게 보도록 고쳤다
  - 두 표기를 모두 등록한다. 0건인 상태값은 칩이 렌더되지 않으므로 시트가 어느 쪽을 쓰든 안전하다
  - 수정 후 실데이터에서 상태 칩 합계 93 = 행수, `In-progress` step = `요청 done → KYB active` 확인

#### 검증 결과 (2026-09-11 기준)
| 항목 | 시트 재현 | 백엔드 페이로드 | |
|---|---|---|---|
| KRW 온보딩 행 | 127 (128 − VND수납 1) | 127 | ✅ |
| VND 온보딩 행 | 93 | 93 | ✅ |
| KRW 상태 분포 | Succeeded 80 / Failed 34 / KYC 11 / Ops 2 | 동일 | ✅ |
| VND 상태 분포 | Approved 71 / Withdrawn 15 / In-progress 4 / Rejected 3 | 동일 | ✅ |
| KRW `SLA` · 판정일 채워짐 | 112 · 114 | 112 · 114 | ✅ |
| KRW 입금 | 92곳 · ₩45,393,089,339 · 1,356건 | 동일 | ✅ |
| KRW 머천트별 체크섬 | `be284445:3967` | `be284445:3967` | ✅ |
| KRW RAW 정산 | 1,356 + 제외 65 = 1,421행 / 합계 일치 | — | ✅ |
| VND 입금 행 정산 | 847 + 키없음 13 + 구파트너 9,083 + 필수공란 21 = 9,964 | 동일 | ✅ |
| 조용해짐 맵 (KRW) | 7곳 (밴드·경과일) | 7곳 동일 | ✅ |

- 머천트별 체크섬은 금액·건수·**최초 입금일**·최종 입금일을 이어 붙인 다이제스트라,
  일치하면 v7.9의 `depFirst`까지 정확하다는 뜻이다
- VND 금액은 약 $1 차이(142,856,564.14 vs 142,856,563.09, 0.0000007%). **gviz CSV가
  표시 형식으로 반올림해 내려주는 탓**이며 행수는 9,964로 정확히 맞는다.
  KRW는 원 단위 정수라 차이가 없다
- **VND 입금 검증은 gviz로 할 수 없다** — 수취계좌 열이 숫자로 추론되어 19자리 텍스트 VA가
  null로 내려오고, 재현하면 전량 `계좌없음`으로 탈락한다(v7.0 장애와 같은 함정).
  행수 정산으로 우회 검증했다

#### 열린 항목 (코드 수정 아님)
- **승인율 분모에 철회 15건이 들어 있다** — 현재 VND 76%(71/93), 철회 제외 시 91%(71/78).
  `Code.gs`는 `withdrawn`을 따로 분류하지만 프론트는 안 한다. **정의를 정해야 하는 일**이라
  임의로 바꾸지 않았다
- `MCID000470` 중복 2행 여전 (둘 다 Succeeded, 입금 0 → 지금은 점유율 왜곡 없음.
  Slack 스냅샷 키 충돌은 남는다)
- KRW 고아 27곳 — 온보딩 탭에 없는데 입금이 있다. 2024~2025 이력 유입과 연결된 사안
- 현재 리드타임 실측: KRW 중위 4일 / p90 54일 / 최장 191일, 활성화 중위 3일 · 30일 내 75% ·
  대기 7곳. VND 활성화 중위 2일 · **30일 내 36%** · **대기 35곳**

### v7.10 — 2026-09-11
- BACKEND_URL 업데이트 — v7.9(`Lead Time`·`조용해짐` 칩)와 아래 수납 통화 제외를 실은 배포 반영.
  교체 전 엔드포인트 확인: 토큰 없이 `{"error":"forbidden"}` — 인증 게이트 정상
- **KRW 탭의 VND 수납 건 제외** — 시트에 `Collection Type` 컬럼이 생기며 두 수납 통화가 섞였다.
  코리도 보드는 수납 통화로 갈라야 혼란이 없다
  - 현재 대상 1건: `ZHIYE LIMITED` / `MCID000499` (Jivara, Succeeded, 시트 123행)
  - **이 머천트는 이미 VND 탭에 `VN0163`으로 있다** (Jivara · H-PAY · Approved · 19자리 VA).
    KRW 탭 이중 기록이므로 빼도 SMOS에서 사라지지 않고 VND 보드에서 보인다
  - KRW 입금(`[RAW] KRW data`)은 0건이라 `온보딩 기록 없음` 고아 행으로 튀지 않는다
  - `MAP.KRW.excludeWhen = { header, values }` 설정으로 두고, 판정은 `excluder_()` 한 곳에 둔다.
    **보드(`readTab`)와 Slack(`pollStatusChanges`)이 각자 탭을 읽으므로** 한쪽만 걸러지면
    보드에 없는 머천트로 알림이 온다. 장기 체류·주간 보고는 `readTab` 결과를 받아 자동 반영
  - 컬럼이 없으면 아무것도 가리지 않는다 — 시트가 되돌아가도 조용히 전부 통과한다
  - 제외 건수는 로그에 남긴다. 보드 행수가 시트와 안 맞을 때 여기부터 본다
- **`Collection Type`이 2번 열로 삽입되며 뒤 컬럼이 모두 +1 밀렸다.** 온보딩 탭은
  `findCol_`로 헤더명 매칭이라 **코드 수정이 필요 없었다.** `[RAW] KRW data`는 고정 인덱스를
  쓰지만 그 탭은 바뀌지 않아 `keyCol 4 / amount 6 / date 5 / vaType 15`가 전부 유효하다 (재확인)
- 검증: 실제 헤더 배치 그대로 하니스 13건 통과 — 제외 대상, 대소문자·공백 변형, 공란 통과,
  컬럼 부재 시 전량 통과, 밀린 위치에서 `SLA`·판정일 매칭, Slack 폴링 제외와 스냅샷 미기록

### v7.9 — 2026-09-11
로드맵 3·5·6번을 함께 배포. 4번(주간 보고 + 승인 후 입금 없음)은 이번 범위에서 제외.

- **`조용해짐` 칩** — 입금 중단 경보 대상을 보드에서도 본다. 행 서브라인 + 상세에 `조용 40일`
  - **판정은 백엔드가 한다.** Slack 경보와 같은 함수(`quietRows_`)의 결과를
    `_dep[cur].quiet = {머천트키: [구간, 경과일]}`로 내려준다. 화면이 다시 계산하지 않으므로
    "칩은 떴는데 알림은 안 왔다"가 생기지 않는다. 구간을 조정하면 두 곳이 같이 움직인다
  - 기간 필터를 타지 않는다 — 경보는 항상 오늘 기준이다
  - `온보딩 기록 없음` 그룹(고아 행)에도 붙는다. 온보딩 탭에 없는 머천트가 오히려 방치되기 쉽다
  - 색은 거절(빨강)이 아니라 주의(주황, `--s-vaw-*`). 실패가 아니고 후속 대상이라는 뜻이다

- **`Lead Time` 탭** — 로드맵 5(SLA 뷰)와 6(활성화 리드타임)을 **한 탭에 합쳐** 배포
  - 합친 이유: 요청→심사(우리 속도)와 심사→첫 입금(활성화)은 "신청부터 돈까지"라는 한 질문의
    앞뒤 구간이다. 탭을 나누면 둘을 같이 보지 않는다. 나누면 탭이 7개가 되어 폰에서
    두 개가 스크롤 밖으로 밀리는 문제도 있었다 (v7.5에서 되찾은 헤더를 다시 잃는다)
  - **1부 · 온보딩 소요일** (KRW만): 중위값 · p90 · 최장 → 클라이언트별 중위값 →
    월별 중위값 → 60일 초과 목록
    - **평균을 쓰지 않는다.** 191일 한 건이 평균을 혼자 끌어올려 나머지의 실상을 가린다
    - 시트가 계산한 `SLA` 컬럼을 읽기만 한다. 백엔드가 다시 계산하지 않는다
    - **표본 3건 미만 클라이언트는 막대에서 숨긴다.** 1~2건의 중위값은 추세가 아니다
    - 월별은 **심사가 끝난 달**로 묶는다(`Compliance approval or failure.`, 공란이면 요청일).
      "지금 나아지고 있나"에 답하는 축은 완료 시점이다
    - 막대 색이 임계를 말한다: 60일+ 빨강 / 30일+ 주황 / 그 아래 파랑.
      1일 대 191일이면 빠른 쪽이 1.6%로 안 보이므로 최소 폭 3%를 준다
    - VND는 요청일이 40/93건뿐이라 계산하지 않고 그 이유를 화면에 적는다. 추정하지 않는다
  - **2부 · 활성화 리드타임**: 중위 활성화일 · 30일 내 활성화율 · **30일 넘게 첫 입금 없는 목록**
    - 마지막 항목이 세일즈가 실제로 전화할 대상이다. 표를 누르면 그 머천트로 이동한다
    - 기준일은 코리도별로 `CORRIDORS.actAnchor`에 둔다 — KRW는 VA 발급일이 시트에 없어
      **심사 판정일**, VND는 **VA 발급일**. 하드코딩 분기를 만들지 않았다
    - **30일 내 활성화율의 분모는 "기준일로부터 30일이 지난 곳"이다.** 이번 주 승인 건까지
      넣으면 아직 기회가 없던 곳이 미활성으로 세어져 비율이 늘 내려간다
    - 대기 목록은 연도를 가리지 않는다 — 작년 승인 건이 아직 안 들어온 게 가장 급하다
    - 기준일보다 첫 입금이 앞서는 곳(온보딩 기록 전부터 거래)은 0일로 접고 몇 곳인지 밝힌다
  - **기간 필터를 타지 않는다.** 저 선택은 매출용이고 목록 화면에만 보이는데, 연도 목록이
    입금 있는 연도로만 만들어진다(`depYears`). 입금은 2024년부터인데 온보딩은 2026년부터라
    2024를 고르면 이 탭이 통째로 비는 구멍이 있었다. 시간축은 `월별 중위값`이 담당한다
  - 탭이 6개가 되어 폰에서 넘치므로, 선택된 탭이 화면 밖이면 가로 위치만 당겨 보이게 한다
    (`scrollIntoView`는 세로로도 움직여 헤더가 튄다). 코리도를 바꿔 없어진 뷰는 목록으로 되돌린다

- **백엔드**: 머천트별 **최초 입금일**(`depFirst`) 추가 — 월 내역으로는 첫 "달"만 알 수 있다.
  KRW `MAP`에 `sla`·`decisionDate` 추가. 두 필드는 `CORRIDORS.columns`에 넣지 않았으므로
  목록·에디터·CSV에 노출되지 않는다 (§8 `건별 SLA 타이머` 비목표 유지)
- 검증: 백엔드 Node 하니스 15건 통과 — 최초 입금일, `SB ` 접두사 합산, 구 파트너 제외,
  `조용해짐 맵 = 경보 대상` 일치, 180일 초과 제외. 화면은 13행 픽스처로 중위값 4일 / p90 120일 /
  최장 191일 / 중위 활성화 5일 / 30일 내 60%(6/9→10곳 분모) / 대기 3곳이 모두 손계산과 일치.
  미래 판정일·SLA 공란·승인 전 거래 3개 경계 케이스 처리 확인.
  코리도 2 × 뷰 6 = 12조합 렌더 에러 0, 가로 오버플로 0, 목록 헤더 312px(38%) 유지, 다크·EN 확인

### v7.8 — 2026-09-11
- BACKEND_URL 업데이트 — v7.4(장기 체류)·v7.6(입금 중단)·v7.7(월 내역) 백엔드 배포 반영

### v7.7 — 2026-09-11
- **머천트별 입금액 연·월 필터** — 세일즈가 기간을 골라 본다. **디폴트는 올해**
  - 원천에 2024년부터 들어오면서 전체 누적만 보여주면 "올해 얼마 들어왔나"를 알 수 없게 됐다
  - 백엔드가 머천트별 월 내역(`depM = {"2026-08":[금액,건수,그달마지막입금일]}`)을 내려주고
    화면에서 기간을 다시 계산한다. **백엔드 재호출이 없다**
  - 연도 목록은 데이터에 실제로 있는 연도만 — 없는 연도를 고르게 하지 않는다.
    올해 데이터가 없으면 최신 연도로 떨어진다
  - 적용 범위: 행 배지·상세·그룹 합계·`입금순` 정렬·`미입금` 판정·`입금 전환`·집중도 전체·CSV
  - **추이는 연도만 따른다.** 한 달로 좁히면 막대가 하나 남아 추이가 아니게 되므로,
    월을 고르면 카드에 `추이는 연도 기준`을 표시한다
  - `온보딩 기록 없음` 그룹도 기간을 따른다 — 이 그룹은 "온보딩 탭에 없는 입금"을 드러내려고
    있으므로 선택 기간에 입금이 없으면 드러낼 게 없다
  - **경보(장기 체류·입금 중단)는 이 필터와 무관하다.** 화면 상태가 알림을 바꾸면 안 된다
- `CSV 내보내기`를 더보기 메뉴로 이동 — 기간 선택 2개가 들어오며 툴바가 다시 2줄(85px)이 됐다.
  폰에서 CSV를 쓸 일이 거의 없어 JSON 백업과 같은 자리로 보냈다. 툴바 45px 한 줄 유지
- 검증: 2024~2026 이력 픽스처로 연도별·월별 합계가 각각 검산과 일치.
  집중도 상위 1곳이 2026=92.3%(Unifi) / 2025=98.0%(Arpalm) / 전체=55.1%로 기간에 따라 바뀌고,
  추이 월 목록이 연도로 좁혀지는 것 확인. 헤더 314px(39%) 유지, JS 에러 0

### v7.6 — 2026-09-11
- **입금 중단 경보** (`sendQuietAlert`) — 거래하던 머천트가 멈춘 것을 알린다. 일일 트리거 09:40 KST
  - **볼륨 구간별 임계일**: 상위(누적 80% 이내) 14일 / 중간(~99%) 30일 / 하위 60일.
    경과일만 보면 24일 된 ₩142.8억이 안 뜨고 117일 된 ₩200만이 뜬다 — 급한 순서가 뒤집힌다
  - 구간 경계는 **누적 점유율**이라 금액을 하드코딩하지 않는다. 코리도가 커지면 기준도 따라 움직인다
  - 구간은 그 머천트를 더하기 **전**의 누적으로 정한다. 더한 뒤로 보면 경계를 걸치는 머천트가
    한 단계 아래로 밀려 점유율 38%인 2위가 중간 구간으로 떨어진다
  - **하위 구간은 꺼 둔다** — 첫 발송 범위를 상위·중간으로 한정. 목록이 소화된 뒤에 켠다
  - `QUIET_MAX_DAYS = 180` — 이보다 오래 조용한 건 "멈춘" 게 아니라 **"끝난" 관계**다.
    원천에 2024년 이력이 들어오면서 300·464일 무입금 건이 볼륨 상위에 배치돼 매번 올라오는
    문제가 생겼다. 이탈 경보는 최근까지 거래한 곳만 대상으로 한다
  - 반복 억제는 v7.4와 같은 단계 승격 + 스냅샷(`deposit_quiet_snap`).
    입금이 재개되면 추적에서 빠지고, 다시 멈추면 처음 단계부터 알린다
  - 금액 큰 순으로 정렬 — 세일즈가 위에서부터 전화하면 된다
  - `previewQuietAlert()` — 발송 없이 로그로만 대상을 확인하는 함수. 첫 발송 전 점검용
- **`SLACK_WEBHOOK_URL_SALES`** 선택 속성 추가. 비어 있으면 기존 웹훅으로 폴백.
  채널을 나누지 않기로 했으므로(하루 3~6건은 음소거할 물량이 아니고, 세일즈향이 하루 1건이면
  별도 채널은 오히려 안 열린다) 속성만 준비해 두고 비워 둔다 — 나중에 채우면 코드 변경 없이 분리된다
- `sendSlack_(payload, url)` — 두 번째 인자로 대상 웹훅을 받게 함 (기본값은 기존 동작)
- `DEPOSITS`에 `cur` 추가. 경보 메시지의 통화 표기용 (KRW 억/만 · VND $M/K)
- 검증: `scratchpad/quiet_test.js` 17개 단정 — 구간 배정, 180일 가드, 하위 off,
  단계 승격, 입금 재개 시 침묵, 웹훅 폴백

### v7.5 — 2026-09-11
- **모바일 헤더 정리** — 375×812에서 헤더 **421px(화면 51%) → 314px(39%)**,
  첫 머천트가 y=433 → **y=326**. 기획서가 모바일 우선이고 핵심 가치가 "3초"인데
  반 화면을 스크롤해야 머천트가 나오던 상태를 고쳤다
- **뷰 전환을 한 줄로 통합** — `Status/Forecast` 밴드 + `대시보드` 버튼 + `Merchants/Concentration/Trend`
  탭이 세 군데 흩어져 있었다. `#viewNav` 하나로 합쳐 밴드 하나(42px)만 남긴다
  - 항목: `Merchants · Concentration · Trend · Dashboard · Forecast` (폭이 좁으면 가로 스크롤)
  - 입금 데이터가 없는 코리도에서는 `Concentration`·`Trend`를 항목에서 뺀다 (VND는 3개만 표시)
  - 목록이 아닌 화면에서는 검색·칩·툴바를 모두 내린다. 대시보드도 검색을 쓰지 않으므로 포함
- **더보기 메뉴 신설** (`#moreMenu`) — 테마·언어·JSON 백업·주간 보고·로그아웃을 옮겼다
  - 폰에서 언어 토글이 **화면 밖으로 잘리고 있었다**. 브랜드 줄이 70 → 38px로 줄고 잘림도 해소
  - 툴바는 `입금순`·`CSV 내보내기` 둘만 남겨 2줄(80px) → 1줄(43px)
  - `display`를 클래스로 주면 `hidden` 속성을 덮어써 메뉴가 처음부터 열려 보인다 → `.more-menu[hidden]` 추가
- 모바일에서 긴 제목(`Sub Merchant Onboarding Status`)을 감춘다. 왼쪽 `SMOS` 마크가 이미 앱 이름을 말한다
- 검증: 375×812에서 5개 뷰 전환·메뉴 개폐·코리도별 항목 수·KR/EN 전환 확인. JS 에러 0

### v7.4 — 2026-09-11
- **장기 체류 알림** (`sendStaleAlert`) — 같은 상태로 **14일** 이상 머문 건을 Slack 알림
  - 일일 트리거 09:30 KST. 단 **새로 단계를 넘은 건만** 보낸다
  - **반복 억제는 단계 승격으로 한다** — T·2T·4T…를 넘는 순간에만 한 번씩.
    108일 체류 건은 14·28·56·112일에 네 번 알리고 그 사이에는 조용하다.
    조건이 유지되는 동안 매일 보내면 같은 줄이 107번 오고, "사람이 처리하면 목록에서 빠진다"에
    기대는 건 책임 전가다. **알림의 반복 여부는 사람의 처리와 무관하게 시스템이 판단해야 한다**
  - 스냅샷(`stale_alert_snap`)은 체류 중인 건만 유지한다. 종결되면 자동으로 빠지고
    같은 상태로 재발하면 처음 단계부터 다시 알린다
  - 새로 넘은 게 없는 날은 발송하지 않는다 (매일 "0건"은 소음)
  - 종결 상태(`success`+`failed`+`withdrawn`)가 아닌 것 전부를 본다. `CORRIDOR_STATUSES.progress`
    목록에 빠진 KRW `Inquiries`도 방치될 수 있으므로 그 목록에 의존하지 않는다
  - 요청일이 비면 건너뛴다 — 경과일을 추정하지 않는다
  - 기준 14일 근거: 미완결 8건 경과일 108·23·23·15·2·1·1·1일 → 7일 4건 / 14일 4건 / 21일 3건 / 30일 1건
- `depDate_()`에 시트 한국 로케일 형식 `2026. 5. 26` 추가. `daysBetween_()` 신규 (UTC 기준, DST 무영향)
- 검증: `stale_test.js` 18개 + `escalation_test.js` 14개 단정.
  후자는 120일을 하루씩 돌려 알림이 **4번**만 나가는지, 같은 날 재실행이 침묵하는지,
  종결 후 재발 시 다시 알리는지 확인한다
- **기획 범위 결정 3건** (§3, §8)
  - SMOS가 **조회**와 **분석** 두 일을 한다는 것을 명시. 판단 기준을 대상별로 분리
  - §8의 SLA 비목표를 **머천트별 실시간 타이머**로 좁힘. 집계 SLA 리포트는 만든다
  - **심사자별 처리량은 만들지 않는다** — 데이터는 있으나 소수 조직에서 사람별 지표는 행동을 왜곡한다
- §7을 온보딩 효율 로드맵으로 재작성 (P1~P5 + 앱 밖 선행 과제)

### v7.3 — 2026-09-11
- **보안 리뷰 수정 3건**
  - 진단 탈락 샘플이 수취계좌번호를 실행 로그·스크립트 캐시에 남기고 있었다. `depSample_()`로
    6자리 이상 숫자는 값 대신 타입·자릿수만 남긴다 (진단에 필요한 건 애초에 자릿수였다).
    `_diag`는 HTTP 응답에 들어가지 않으므로 보드 사용자에게 노출된 적은 없다
  - 행 `data-id`가 이스케이프 없이 속성에 들어갔다. id가 `uid()`뿐일 때는 무해했지만
    고아 머천트 행 id가 시트 값 기반이라 노출 경로가 생겼다. 문자열로 조립한 `querySelector`도
    `dataset` 비교로 교체
  - CSV 수식 주입 방어 추가 — `= + - @`로 시작하는 값 앞에 `'`를 붙인다. 숫자는 제외해
    음수 금액이 깨지지 않게 함 (v2.0부터 있던 문제)
- 앱이 참조하지 않는 아이콘 이미지 3개를 퍼블릭 레포에서 제외 (로컬 유지, gitignore)
- BACKEND_URL 업데이트

### v7.2 — 2026-09-10
- **집중도 표에 FI/클라이언트 컬럼** — 코리도의 group 역할 컬럼을 그대로 쓴다 (KRW `Client` / VND `FI Merchant`)
- **집중도 표 행 클릭 → 머천트 상세로 이동** — 검색어를 해당 MID(없으면 이름)로 채우고 그 행을 펼쳐
  상태·티켓을 바로 보여준다. sticky 헤더 높이만큼 offset을 줘 이름이 가려지지 않게 함
- 추이 주석의 최대 머천트를 정규화 키(`CLOUDWAVEHONGKONGLIMITED`) 대신 실제 이름·MID로 표시
- KR 조사 오류 수정 — 값이 끼워지는 자리 뒤의 `은/는`은 고를 수 없어 문장 구조를 바꿈
- **뷰 전환 토글은 KR에서도 영어** (`Merchants · Concentration · Trend`) — 같은 헤더의
  `Status/Forecast`·`KRW/VND`가 이미 영어 단일 표기라 하나만 한글이면 오히려 어색하다.
  카드 제목·표 헤더·KPI·주석은 그대로 번역된다

### v7.1 — 2026-09-10
- **입금 분석 뷰 2종** — Status 화면에 `머천트 · 집중도 · 추이` 토글 추가
  - **집중도**: 상위 1곳·5곳 점유율, 80% 도달 곳수, 머천트/클라이언트별 점유율, 상위 15곳 누적 표
  - **추이**: 월별 입금액(구 파트너·귀속 불가 분리 표시), 월별 입금 머천트 수(기존/신규), 월별 상세 표
  - KRW는 `최대 머천트 제외` 토글 — 특정 MID를 박아 넣지 않고 백엔드가 계산한 최대 머천트를 쓴다
- **머천트 행에 점유율 표시** (총액 아래), 상세에는 소수 2자리
- **`입금순`이 기본 정렬** — 입금이 붙은 뒤로는 "얼마 들어왔나"가 첫 질문이다
- **`온보딩 기록 없음` 그룹** — 입금은 있는데 온보딩 탭에 행이 없는 머천트를 목록 맨 아래 별도 그룹으로.
  상태값이 없어 클라이언트 그룹에 섞으면 승인율·퍼널 수치가 오염된다
  - 클라이언트 라벨은 RAW 탭의 `FI / Direct`를 보고 보드의 FI Merchant 체계(`Direct-Merchant`)에 맞춘다
- **점유율 중복 집계 수정** — 같은 입금 키가 보드에 두 행이면(KRW `MCID000470` 중복 입력)
  같은 돈이 두 번 세어져 점유율 합이 100%를 넘었다. 조인 키 단위로 접어서 센다
- 백엔드가 `_dep`로 월별 시계열·최대 머천트 기여분·구 파트너 계열·고아 머천트를 내려준다
  - 귀속 불가 금액은 `sum`에 섞지 않고 `u`로 따로 — 섞으면 점유율 합이 100%를 넘고, 버리면 그 달이 사라진다
  - 검증: `scratchpad/dep_test.js` 합성 데이터 27개 단정. 월별 합계가 총액·귀속불가·구파트너와 각각 일치

### v7.0 — 2026-09-10
- **머천트 입금액 표시** — 온보딩이 실제 거래로 이어졌는지 보드에서 확인
  - 머천트 행에 입금 배지(총액 · 건수 · 최근 입금일), 상세에 입금 4개 항목
  - `입금순` 정렬 추가 (그룹순 → 최근순 → 입금순 순환)
  - `미입금` 필터 — 승인 완료인데 입금 0건. 행 왼쪽 주황 띠로 구분
  - `입금 전환` 지표 (승인 대비 입금 발생 비율)
  - 그룹 헤더에 클라이언트별 입금 합계, CSV에 입금 3열 추가
- **원천**: `[RAW] KRW data`(거래 단위, MID 조인) / `[RAW] VND data`(거래 단위, 머천트명 조인)
  - VND는 Baokim(구 파트너)·H-PAY(현 파트너)가 섞여 있어 **수취 VA 자릿수**로 분리
    (H-PAY = `9631…` 19자리 854건 $9,299,579 / Baokim = `9633`·`9634` 계열 10~14자리 9,083건)
    - 19자리는 시트의 15자리 한계를 넘어 **텍스트로 저장**된다. gviz·QUERY로 읽으면 이 열이
      number로 추론되어 텍스트 셀이 null로 내려오므로 "계좌 공란"처럼 보인다. 이 착시를 믿고
      공란 판정으로 가르면 Apps Script에서는 전량 탈락한다 (v7.0 초기 배포 장애 원인)
  - VND 머천트명이 `SB ` 접두사로 이중 기록되므로 접두사를 떼고 합산
  - 통화가 달라(KRW 원화 / VND USD) 코리도 간 합산은 하지 않음
- `readDeposits_()` + `CacheService` 5분 캐시, `debugDeposits()` 진단 함수 추가
  - 탈락 사유별 건수·금액을 집계해 원천 총액과 대조한다. VND 검증: 채택 841 + 구파트너 9,083
    + 잘린행 21 + 이름공란 13 = 9,958건, 금액 합 $142,769,441.38 = 시트 Volume(USD) 총합
- BACKEND_URL 업데이트

### v6.3 — 2026-08-03
- **Slack 스냅샷 키 안정화** — 행 번호 기반(`VND-145`) → MID 기반(`VND:VN0145`) 키로 변경, 행 삽입/삭제 시 알림 누락 방지
- BACKEND_URL 업데이트

### v6.2 — 2026-08-03
- **Slack 알림에 FI Merchant 표시** — 상태 변경 알림에 Client/FI Merchant 정보 추가
- Slack 메시지 생성 로직 config 기반으로 정리 (5개 타입 중복 제거)
- BACKEND_URL 업데이트

### v6.1 — 2026-08-02
- **보안: JWT fallback 제거** — tokeninfo API 실패 시 서명 미검증 fallback을 제거하고 인증 거부로 변경
- BACKEND_URL 업데이트

### v6.0 — 2026-08-02
- **P1: 머천트 카드 step tracker 항상 노출**
  - step tracker를 detail 내부 → item-row 아래로 이동 (접힌 상태에서도 보임)
  - stepTag() 함수·CSS 제거 (중복)
- **P2: 대시보드 퍼널 통계**
  - Summary 카드 3개 (Total / Succeeded / 완료율)
  - 상태별 가로 바 차트 (전체 대비 비율 %)
  - Failed/Rejected 구분선 분리
- KR/EN 다국어 키 추가 (dashFunnelSection, dashFunnelTotal, dashFunnelRate)
- SPEC.md 기획안 추가

### v5.1 — 2026-07-31
- Slack 알림을 onEdit → 1분 폴링 방식으로 전환
  - IMPORTRANGE 데이터는 onEdit 트리거가 발동하지 않는 문제 해결
  - `pollStatusChanges()`: 전체 시트 읽기 → ScriptProperties 스냅샷과 diff → 변경 건 알림
- BACKEND_URL 업데이트

### v5.0 — 2026-07-14
- Slack 실시간 알림 도입 (5가지 유형)
- 주간 보고 Slack 발송
- Container-bound script 마이그레이션

### v4.2 — 2026-07-12
- Forecast 뷰: KRW/VND 별도 탭에서 읽기
- 현황→Status 이름 변경

### v4.1 — 2026-07-10
- VA 컬럼 매핑 수정 (`"VA"` → `"가상 계좌 여부"`)
- VND 은행명 필드 추가

### v4.0 — 2026-07-08
- 주간 보고 이메일 (HTML 템플릿)
- Ops/Sales 분리 이메일
- Forecast 뷰 추가
- 한국어/영어 전환

### v3.0 — 2026-07-05
- Google Sign-In 도입 (sentbe.com 제한)
- Users 탭 기반 role 관리
- JWT 검증
- GitHub Actions 보안 스캔

### v2.0 — 2026-06-28
- 대시보드 (주간·월간 차트, 승인율)
- 다크 테마
- CSV 내보내기, JSON 백업
- 정렬 모드

### v1.0 — 2026-06-20
- 최초 배포
- KRW/VND 탭, 검색, 상태 필터
- 클라이언트 그룹, 머천트 상세
- Step tracker, VA 복사
